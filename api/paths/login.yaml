post:
  tags:
    - User
  summary: Login
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/UserCredential.yaml"
  responses:
    "200":
      description: Ok
      content:
        "application/json":
          schema:
            $ref: "../schemas/UserInformation.yaml"
    "401":
      description: Login failed
    "500":
      description: Internal failure
